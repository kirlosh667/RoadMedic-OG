# RoadMedic-OG
RoadMedic is an Android app for reporting potholes with photos, GPS location and severity. It uses Fused Location API for location, OSMDroid Maps API for maps, and Firebase for storing reports and images in the cloud, enabling real-time data access and better road maintenance tracking.
